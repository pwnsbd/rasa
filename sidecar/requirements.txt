# Sidecar dependencies. torch is installed separately by scripts/setup-sidecar.js
# (CUDA build if a GPU is detected at setup time, CPU build otherwise) since the
# right wheel depends on the machine.
fastapi==0.115.5
uvicorn[standard]==0.32.1
pillow==11.0.0
numpy==2.1.3

# Added once the real extraction/generation pipeline lands (spec §2.2),
# replacing sidecar/essence.py's mock-palette technique:
#   diffusers, transformers, accelerate, safetensors, torch (see above)
